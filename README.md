1. create the repo first 
2. connect it with local staging 
3. give a git commit push 
4. install dvc 
5. init dvc ( command: DVC init )  -> creates .dvc .dvcignore 
6.  creating a s3 forlder (s3 bucket) 
7.  ''' dvc remote add -d myremote S3 '
8. dvc add data/ 
9. Now "dvc commit' and "dvc push"
10. Do a git commit push to mark this stage as first version of data. 

